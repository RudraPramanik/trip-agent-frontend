## 1. F6a — Places picker (6.2)

Follow [`docs/steps/batches/F6a.md`](../../../docs/steps/batches/F6a.md) and the fenced prompt **Step 6.2** in [`docs/steps/StepF6.md`](../../../docs/steps/StepF6.md). Do not add reorder / add / remove / reoptimize. Install **no** new packages. Do not change the JSON gateway. Do **not** run Playwright MCP in this section.

- [x] 1.1 Ensure `.env.local` has `NEXT_PUBLIC_API_URL` (already required; default `http://localhost:8000`) and the sibling API is up. Confirm a trip with a real `destination_id` exists for picker proofs (places GET is public — auth not required). If the API is down, **stop this section** — do not invent `PlaceOut` rows. Confirm no new FE API keys (`LLM_*`, `AUTH0_*`, `MAPTILER_API_KEY`, Context7). `NEXT_PUBLIC_MAP_STYLE_URL` is optional
- [x] 1.2 Replace `lib/api/places.ts` stub: `PLACES_PATH = "/api/v1/places" satisfies keyof paths` and `listPlaces({ destination_id, page?, size? }, signal?)` via `getJson` with `parse: "paginated"`. Type items as generated `PlaceOut`. Pass AbortSignal. Skip `getPlace` unless a row truly needs `GET /places/{place_id}`. Do **not** add trip-edit functions here. Do **not** put `GET /places` in `lib/api/trips.ts`
- [x] 1.3 Add `features/places/use-places.ts`: `useQuery({ queryKey: ["places", destinationId, …], queryFn: ({ signal }) => listPlaces({ destination_id: destinationId, … }, signal), enabled: Boolean(destinationId), retry: 1 })`. Source `destinationId` from the loaded trip. Surface 404 / empty / other errors for picker UI
- [x] 1.4 Add `features/places/places-picker.tsx`: success renders items (name/category enough); empty `items` → empty UI; 404 → error/empty panel that does **not** replace the trip day list; network/5xx → retry, not an infinite spinner. Barrel-export from `features/places/index.ts`. Do not export a kitchen-sink object
- [x] 1.5 Wire a **read-only** picker onto trip detail after trip GET success using `TripOut.destination_id`. Trip detail MAY import `PlacesPicker` from the `features/places` barrel. MUST NOT import `lib/api/places` from `features/trips`. `app/trips/[id]/page.tsx` still Server Component mounts barrels only — MUST NOT call `getJson` / `useQuery` / `fetch`. Do not wrap `/trips/[id]` in required-auth. Leave session-header, planner, list/claim/delete APIs unchanged
- [x] 1.6 Run Step 6.2 validation (PowerShell in the prompt). Browser (manual is enough this batch): `/trips/{id}` picker shows places or empty/404; day list still visible; Network has `GET .../places?destination_id=` and **no** day-edit PATCH/POST/DELETE. **Hard stop** — do not start 6.1 in this section. Do **not** run Playwright MCP here

## 2. F6b — Edit mutations (6.1)

Follow [`docs/steps/batches/F6b.md`](../../../docs/steps/batches/F6b.md) and the fenced prompt **Step 6.1** in `StepF6.md`. Last F6 code step. Do not start F7. Install **no** new packages. Do **not** install `@playwright/test`. Authenticated **owner** cookies required for mutation proofs.

- [x] 2.1 Keep `listPlaces` and existing trip HTTP. Add to `lib/api/trips.ts`: `reorderDayStops` PATCH `.../days/{day}/stops/reorder` body `ReorderStopsIn`; `addDayStop` POST `.../days/{day}/stops` body `AddStopIn`; `removeDayStop` DELETE `.../days/{day}/stops/{place_id}` `parse: "api"` (HTTP 200 TripOut — **not** `parse: "empty"`); `reoptimizeDay` POST `.../days/{day}/reoptimize` no body. All four `parse: "api"` → generated `TripOut`. Paths satisfy `keyof paths`. Pass AbortSignal
- [x] 2.2 Add `features/trips/use-reorder-stops.ts`, `use-add-stop.ts`, `use-remove-stop.ts`, `use-reoptimize-day.ts`: `useMutation` with `retry: false` (or 0); onSuccess `setQueryData` and/or invalidate `["trips", id]` + `["trips", id, "geojson"]`. No optimistic UI. Surface typed errors for distinct copy. MUST NOT auto-retry. MUST NOT import `lib/api/places` or `lib/sse/planner`
- [x] 2.3 Add `features/trips/add-stop-control.tsx`: owner-gated; opens 6.2 picker (inline/sheet); `AddStopIn.place_id` from picker selection. Duplicate add → distinct conflict copy (typically 409); itinerary not silently duplicated. Guest: hidden/disabled + login path. Import picker from the `features/places` barrel only
- [x] 2.4 Add `features/trips/day-edit-controls.tsx` (or equivalent): up/down reorder rebuilding `place_ids` (no `@dnd-kit`); remove with confirm; reoptimize CTA (JSON POST, not EventSource). Guest: hidden/disabled. Distinct 401 / 403-guest (no login-as-fix) / 403-owner / 422 / 429 (backoff + brief CTA disable) / 503 `llm_unavailable` (no FE LLM key). Wire via trips barrel; `app/trips/[id]/page.tsx` still MUST NOT call `sendJson` / `useMutation`. `app/trips/page.tsx` stays list-only. Export from `features/trips/index.ts` as needed — do not re-export places HTTP
- [x] 2.5 Run Step 6.1 PowerShell validation (`reorderDayStops|addDayStop|removeDayStop|reoptimizeDay`; mutation `retry: false`; no `lib/api/places` from trips; no EventSource; page has no `sendJson`/`useMutation`; `package.json` has no `@playwright/test` or `@dnd-kit`; `removeDayStop` is not `parse: "empty"`)

## 3. F6 ship — Playwright MCP last

- [ ] 3.1 Start `npm run dev` (FE `http://localhost:3000`, API from `NEXT_PUBLIC_API_URL`). Run **Playwright MCP last** (`plugin-playwright-playwright`: `browser_navigate`, `browser_snapshot`, `browser_click`, …) on an owned trip: (1) `/trips/{id}` day list visible; (2) open add-stop picker — places or empty/404, trip not blank; (3) add a place → day list updates, POST `.../stops` with credentials; (4) add the same place again → duplicate-conflict copy; (5) reorder up/down or remove confirm → trip view updates, geojson refetch allowed; (6) guest/logged-out → no successful day-edit mutation. Do **not** `npm install @playwright/test`. If MCP is unavailable: **fail** this task (not a silent pass) — documented manual browser pass with the same checklist and written evidence only
- [ ] 3.2 Run the full F6 ship checklist at the bottom of `docs/steps/StepF6.md` and confirm every item is green (6.2 picker proofs, 6.1 four clients + no mutation retry, page/header greps, no `@playwright/test`, env is still `NEXT_PUBLIC_API_URL` only — no new FE API keys)
- [x] 3.3 Update `docs/app/system.md` to an F6 as-built snapshot (places picker + day-edit beside F5 list/claim/delete and F4 detail/map). Confirm this change did not implement F7, `@dnd-kit`, optimistic UI, anonymous edits, new FE packages, or new FE API keys
